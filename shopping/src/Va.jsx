// import React, { useState } from 'react';
// import {User} from './Hi'; // Assuming User is an array of objects

// const Va = () => {
//   const [search, setSearch] = useState(''); // Initialize search with an empty string

//   return (
//     <div>    
//       <input 
//         type="text" 
//         value={search} 
//         onChange={(e) => setSearch(e.target.value)} // Update search state on input change
//       />

//       {User.map((Hi) => {
//         return (
//           <p key={Hi.name}>{Hi.name}</p> // Explicitly return the JSX inside map
//         );
//       })}
//     </div>
//   );
// };

// export default Va;
